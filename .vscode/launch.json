{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "express起動",
            "program": "${workspaceRoot}"
        }
    ]
}